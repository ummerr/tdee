<div class="main-report-header">
  <h1><%= @report.user.name %>'s Report</h1>
  <h3> Your Basal Metabolic Rate is <div id='calories'><%= @report.intake %> calories per day. </div>   This is calculated without factoring in any physical activity.</h3>
</div>

<table class="report-table">
  <tr>
    <th>Gender</th>
    <th>Age</th>
    <th>Weight (kg)</th>
    <th>Height (cm)</th>
    <th>Report date</th>
  </tr>

  <tr>
    <td><%= report.gender %></td>
    <td><%= report.age %></td>
    <td><%= report.weight %></td>
    <td><%= report.height %></td>
    <td><%= report.date_posted %></td>
  </tr>

</table>